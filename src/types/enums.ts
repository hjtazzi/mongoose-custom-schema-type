export const enum StaticValues {
  CollectionPrefix = "Test_",
  SaltRounds = 10,
  APINamespace = "api",
  APIVersion = "v1"
}

export const enum PostsType {
  NETWORKS = "networks"
}

export const enum PostStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
  TRASH = "trash"
}

export const enum NetworksViewsType {
  SIMPLE = "simple",
  VISUAL = "visual",
  VOTING = "voting",
  FILE = "file"
}
